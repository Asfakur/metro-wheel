import React from 'react';

const Destination = () => {
    return (
        <div>
            <h2>Destination Private Page</h2>
        </div>
    );
};

export default Destination;<h2>Destination Private Page</h2>